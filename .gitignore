node_modules/
*.log
debugging/logs/
.state/
test/headless/reports/
**/*.zip
dev-prompts.md
.agent-mail/
reports/sweeps/

# ops/ holds anything security-relevant (real hosts, secrets, deploy.env) — never
# committed. Generic templates live in docs/ and ARE tracked.
ops/

# Roblox Studio place files (Rojo is the source of truth)
roblox/*.rbxl
roblox/*.rbxlx
roblox/*.lock
